# SQL_introduction

This project introduces the fundamentals of relational databases and
MySQL: creating and deleting databases and tables, inserting,
updating, and deleting records, using subqueries, aggregate
functions, and ordering/filtering query results.
