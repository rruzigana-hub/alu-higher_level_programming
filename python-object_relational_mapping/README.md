# python-object_relational_mapping

This project covers connecting Python scripts to a MySQL database using
both the raw MySQLdb driver and the SQLAlchemy ORM. It includes scripts
for selecting, filtering, inserting, updating, and deleting records, as
well as writing SQL-injection-safe queries and mapping Python classes
(State, City) to MySQL tables.
