# python-network_1

This project covers making HTTP requests in Python using both the
built-in urllib package and the third-party requests package:
GET/POST requests, reading response headers, handling errors, working
with JSON APIs, and using HTTP Basic Authentication.
