# python-almost_a_circle

This project builds a small class hierarchy (Base, Rectangle, Square)
covering inheritance, private attributes with getters/setters, *args
and **kwargs, JSON serialization/deserialization, and full unit test
coverage using the unittest module.
